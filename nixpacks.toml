[phases.setup]
apt-pkgs = []

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["npx esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=cjs --external:express"]

[start]
cmd = "node dist/index.js"
